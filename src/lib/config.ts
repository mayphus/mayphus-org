import { customKeywords } from './keyword';
import { customHeadline, customHeadlineColor } from './headline';

export const config = {
	uniorgPlugins: [
		customKeywords,
		customHeadline,
		customHeadlineColor,
	],
};
